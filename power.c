#include<stdio.h>
#include<math.h>    

int main(){
    int a;
    printf("Enter value of a:");
    scanf("%d",&a); 
    int ans = pow(a,3);                                 //taking inputs
    printf("The cude of a is: %d",ans);
    return 0;
}